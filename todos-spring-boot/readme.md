# Todos Management App (Spring Boot Sample)

## Commands

### Just Run

`mvn spring-boot:run` or run the `TodosApplication` class from your IDE.

### Build and Run

```bash 
mvn clean package
java -jar target/todos-spring-boot.jar
```
